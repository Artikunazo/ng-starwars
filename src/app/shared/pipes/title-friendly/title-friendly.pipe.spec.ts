import { TitleFriendlyPipe } from './title-friendly.pipe';

describe('TitleFriendlyPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFriendlyPipe();
    expect(pipe).toBeTruthy();
  });
});
